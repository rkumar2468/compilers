int hi()
{
    int x, y;
    x = 10;
    y = input();
    if (y != 10)
    then {
        if (y > 10)
        then
            x = 10;
        else
            x = 19;
        return 11;
    } else {
        print (x);
        return 10;
    }
    // return 11;
}
void main()
{
    hi();
}